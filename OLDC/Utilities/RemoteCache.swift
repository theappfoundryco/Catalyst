//
//  RemoteCache.swift
//  Catalyst
//
//  MOVED. `CacheTTL` and the `RemoteCache` actor now live in
//  `Utilities/NetworkConfig.swift` (already in the build target), so the
//  centralized fetch cache needs no separate pbxproj registration.
//
//  This file is intentionally empty — do NOT redefine those types here, or the
//  build will fail with duplicate symbols. Safe to delete from the project.
//
